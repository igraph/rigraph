% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motifs.R
\name{motifs_randesu_callback}
\alias{motifs_randesu_callback}
\title{Graph motifs with callback}
\usage{
motifs_randesu_callback(
  graph,
  size = 3,
  cut.prob = NULL,
  callback = NULL,
  extra = NULL
)
}
\arguments{
\item{graph}{Graph object, the input graph.}

\item{size}{The size of the motif, currently sizes 3 and 4 are supported in
directed graphs and sizes 3-6 in undirected graphs.}

\item{cut.prob}{Numeric vector giving the probabilities that the search
graph is cut at a certain level. Its length should be the same as the size
of the motif (the \code{size} argument).
If \code{NULL}, the default, no cuts are made.}

\item{callback}{A function to be called for each motif found. The function
should accept three arguments:
\describe{
\item{graph}{The input graph object.}
\item{motif}{A named list with two elements: \code{vids} (the vertex IDs
in the motif) and \code{isoclass} (the isomorphism class of the motif).}
\item{extra}{The \code{extra} argument passed to \code{motifs_randesu_callback()}.}
}
The callback function should return \code{TRUE} to continue the search,
or \code{FALSE} to stop it.}

\item{extra}{An arbitrary R object that will be passed to the callback
function as the \code{extra} argument.}
}
\value{
Invisible \code{NULL}. This function is called for its side effects
(calling the callback function).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
This function is similar to \code{\link[=motifs]{motifs()}}, but instead of counting motifs,
it calls a user-supplied callback function for each motif found.
This is useful for sampling motifs from large graphs or for performing
custom operations on each motif.
}
\examples{
# Collect all triads (3-vertex motifs) in a small graph
g <- make_graph(~ A - B - C - A - D - E - F - D - C - F)
motif_list <- list()
motifs_randesu_callback(g, 3, callback = function(graph, motif, extra) {
  extra[[length(extra) + 1]] <<- motif
  TRUE # continue searching
}, extra = motif_list)
length(motif_list)

# Stop after finding 5 triads
motif_list <- list()
motifs_randesu_callback(g, 3, callback = function(graph, motif, extra) {
  extra[[length(extra) + 1]] <<- motif
  length(extra) < 5 # stop when we have 5 motifs
}, extra = motif_list)
length(motif_list)
}
\seealso{
\code{\link[=motifs]{motifs()}}, \code{\link[=count_motifs]{count_motifs()}}, \code{\link[=sample_motifs]{sample_motifs()}}, \code{\link[=isomorphism_class]{isomorphism_class()}}

Other graph motifs: 
\code{\link{count_motifs}()},
\code{\link{dyad_census}()},
\code{\link{motifs}()},
\code{\link{sample_motifs}()}
}
\concept{graph motifs}
\concept{graph_motifs}
\section{Related documentation in the C library}{\href{https://igraph.org/c/html/latest/igraph-Motifs.html#igraph_motifs_randesu_callback}{\code{motifs_randesu_callback()}}.}

