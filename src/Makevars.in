include sources.mk

LIB_XML ?= $(MINGW_PREFIX)
GLPK_HOME ?= $(MINGW_PREFIX)
LIB_GMP ?= $(MINGW_PREFIX)

PKG_CPPFLAGS=@cflags@ -DUSING_R -I. -Icore -Iinclude/igraph -Ivendor -Ivendor/cigraph/src -Ivendor/cigraph/vendor \
	-DNDEBUG -DNTIMER -DNPRINT \
	-DPRPACK_IGRAPH_SUPPORT -DIGRAPH_THREAD_LOCAL=/**/ \
	-D_GNU_SOURCE=1

PKG_LIBS = -L${LIB_XML}/lib -lxml2 -lz -larpack -lstdc++ -L${GLPK_HOME}/lib \
  -lglpk -lgmp -lgfortran -lquadmath -L$(LIB_GMP)/lib $(BLAS_LIBS) $(LAPACK_LIBS)

OBJECTS=${SOURCES}
