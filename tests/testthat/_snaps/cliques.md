# weighted_cliques(vertex.weights = ) is deprecated but still works

    Code
      res <- weighted_cliques(g, vertex.weights = w, min.weight = 6)
    Condition
      Warning:
      Calling `weighted_cliques()` with positional or abbreviated arguments was deprecated in igraph 3.0.0.
      i Detected call: weighted_cliques(graph, vertex.weights, min.weight)
      i Use instead: weighted_cliques(graph, vertex_weights = , min_weight = )

