# check_deprecated_function() reports a defunct function

    Code
      check_deprecated_function(hub.score)
    Condition
      Error:
      ! `hub.score()` was deprecated in igraph 2.0.0 and is now defunct.
      i Please use `hits_scores()` instead.

